<%= form_with(model: follow, local: true) do |form| %>
  <% if follow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(follow.errors.count, "error") %> prohibited this follow from being saved:</h2>

      <ul>
      <% follow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customerID_id %>
    <%= form.text_field :customerID_id, id: :follow_customerID_id %>
  </div>

  <div class="field">
    <%= form.label :barberID_id %>
    <%= form.text_field :barberID_id, id: :follow_barberID_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
