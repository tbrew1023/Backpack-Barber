<%= form_with(model: product_color, local: true) do |form| %>
  <% if product_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_color.errors.count, "error") %> prohibited this product_color from being saved:</h2>

      <ul>
      <% product_color.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color, id: :product_color_color %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
